/**
 * Type definitions for Microsoft.AzureActiveDirectory resources.
 *
 * **API Version**: 2023-05-17-preview
 * **Generated**: 2025-10-13T00:00:00.000Z
 * **Source**: Microsoft.AzureActiveDirectory Azure ARM Schema
 *
 * @remarks
 * This file is auto-generated. Do not edit manually.
 * Generated by Felix (Schema & Validation Engineer)
 *
 * Azure Active Directory B2C and CIAM (Customer Identity and Access Management) resources.
 *
 * @packageDocumentation
 */

/**
 * B2C Directory SKU properties.
 *
 * @remarks
 * Defines the billing and feature tier for an Azure AD B2C directory.
 */
export interface B2CDirectorySku {
  /**
   * The SKU name.
   *
   * Available values:
   * - Standard: Standard tier with basic features
   * - PremiumP1: Premium P1 tier with advanced features
   * - PremiumP2: Premium P2 tier with full feature set
   */
  readonly name?: 'Standard' | 'PremiumP1' | 'PremiumP2' | any;

  /**
   * The SKU tier.
   *
   * @remarks
   * Currently only A0 tier is available for B2C directories.
   */
  readonly tier?: 'A0' | any;
}

/**
 * B2C Directory properties.
 *
 * @remarks
 * Configuration properties for an Azure AD B2C directory tenant.
 */
export interface B2CDirectoryProps {
  /**
   * The initial domain name of the B2C directory.
   *
   * Constraints:
   * - Length: 3-63 characters
   * - Pattern: Must be globally unique across all B2C directories
   * - Can only contain lowercase letters, numbers, and hyphens
   * - Cannot start or end with a hyphen
   * - Automatically appended with .onmicrosoft.com
   *
   * @example 'contosob2c' results in 'contosob2c.onmicrosoft.com'
   */
  readonly createTenantProperties?: B2CTenantCreationProperties | any;

  /**
   * The SKU of the B2C directory.
   */
  readonly sku?: B2CDirectorySku | any;

  /**
   * The data residency location for the B2C directory.
   *
   * @remarks
   * Determines where user data is stored. Cannot be changed after creation.
   *
   * Available values:
   * - United States: Data stored in US data centers
   * - Europe: Data stored in European data centers
   * - Asia Pacific: Data stored in Asia Pacific data centers
   * - Australia: Data stored in Australian data centers
   */
  readonly location?: 'United States' | 'Europe' | 'Asia Pacific' | 'Australia' | any;

  /**
   * Custom domain configurations for the B2C directory.
   *
   * @remarks
   * Allows configuring custom domains for authentication flows.
   */
  readonly domains?: B2CDomain[] | any;

  /**
   * Billing configuration for the B2C directory.
   */
  readonly billingConfig?: B2CBillingConfig | any;
}

/**
 * B2C tenant creation properties.
 */
export interface B2CTenantCreationProperties {
  /**
   * The initial domain name of the tenant.
   *
   * Constraints:
   * - Length: 3-63 characters
   * - Pattern: Lowercase letters, numbers, and hyphens only
   * - Cannot start or end with hyphen
   * - Must be globally unique
   *
   * @example 'contosob2c'
   */
  readonly initialDomainName?: string;

  /**
   * The country/region code for the tenant.
   *
   * Constraints:
   * - Must be a valid ISO 3166-1 alpha-2 country code
   *
   * @example 'US', 'GB', 'DE'
   */
  readonly countryCode?: string;

  /**
   * The display name of the tenant.
   *
   * Constraints:
   * - Maximum length: 256 characters
   *
   * @example 'Contoso B2C Tenant'
   */
  readonly displayName?: string;
}

/**
 * B2C domain configuration.
 */
export interface B2CDomain {
  /**
   * The custom domain name.
   *
   * Constraints:
   * - Must be a valid fully qualified domain name
   * - Must be verified before use
   *
   * @example 'login.contoso.com'
   */
  readonly name?: string;

  /**
   * Indicates if the domain is the primary domain.
   */
  readonly isPrimary?: boolean | any;

  /**
   * The domain verification status.
   *
   * Available values:
   * - Verified: Domain ownership verified
   * - Unverified: Domain not yet verified
   * - Failed: Verification failed
   */
  readonly verificationStatus?: 'Verified' | 'Unverified' | 'Failed' | any;
}

/**
 * B2C billing configuration.
 */
export interface B2CBillingConfig {
  /**
   * The billing type.
   *
   * Available values:
   * - MAU: Monthly Active Users billing
   * - Auths: Per-authentication billing (legacy)
   */
  readonly billingType?: 'MAU' | 'Auths' | any;

  /**
   * The Azure subscription to link for billing.
   *
   * @remarks
   * Must be a valid Azure subscription ID.
   */
  readonly billingSubscriptionId?: string;
}

/**
 * CIAM Directory SKU properties.
 *
 * @remarks
 * Defines the billing and feature tier for a Customer Identity and Access Management (CIAM) directory.
 */
export interface CIAMDirectorySku {
  /**
   * The SKU name.
   *
   * Available values:
   * - Free: Free tier with limited features
   * - Standard: Standard tier with basic features
   * - Premium: Premium tier with advanced features
   */
  readonly name?: 'Free' | 'Standard' | 'Premium' | any;

  /**
   * The SKU tier.
   */
  readonly tier?: string;
}

/**
 * CIAM Directory properties.
 *
 * @remarks
 * Configuration properties for a Customer Identity and Access Management directory.
 */
export interface CIAMDirectoryProps {
  /**
   * The display name of the CIAM directory.
   *
   * Constraints:
   * - Maximum length: 256 characters
   *
   * @example 'Contoso Customer Directory'
   */
  readonly displayName?: string;

  /**
   * The domain name for the CIAM directory.
   *
   * Constraints:
   * - Must be a valid domain name format
   * - Must be globally unique
   */
  readonly domainName?: string;

  /**
   * The SKU of the CIAM directory.
   */
  readonly sku?: CIAMDirectorySku | any;

  /**
   * The data residency location for the CIAM directory.
   *
   * @remarks
   * Determines where user data is stored. Cannot be changed after creation.
   */
  readonly location?: 'United States' | 'Europe' | 'Asia Pacific' | 'Australia' | any;

  /**
   * Feature settings for the CIAM directory.
   */
  readonly features?: CIAMFeatures | any;

  /**
   * Security settings for the CIAM directory.
   */
  readonly securitySettings?: CIAMSecuritySettings | any;
}

/**
 * CIAM feature settings.
 */
export interface CIAMFeatures {
  /**
   * Enable multi-factor authentication.
   */
  readonly enableMFA?: boolean | any;

  /**
   * Enable conditional access policies.
   */
  readonly enableConditionalAccess?: boolean | any;

  /**
   * Enable identity protection.
   */
  readonly enableIdentityProtection?: boolean | any;

  /**
   * Enable self-service password reset.
   */
  readonly enableSSPR?: boolean | any;

  /**
   * Enable custom branding.
   */
  readonly enableCustomBranding?: boolean | any;
}

/**
 * CIAM security settings.
 */
export interface CIAMSecuritySettings {
  /**
   * Password complexity requirements.
   */
  readonly passwordComplexity?: 'Low' | 'Medium' | 'High' | any;

  /**
   * Password expiration in days.
   *
   * Constraints:
   * - Minimum: 0 (no expiration)
   * - Maximum: 730 (2 years)
   */
  readonly passwordExpirationDays?: number | any;

  /**
   * Enable account lockout after failed sign-in attempts.
   */
  readonly enableAccountLockout?: boolean | any;

  /**
   * Number of failed sign-in attempts before lockout.
   *
   * Constraints:
   * - Minimum: 1
   * - Maximum: 999
   */
  readonly accountLockoutThreshold?: number | any;

  /**
   * Account lockout duration in minutes.
   *
   * Constraints:
   * - Minimum: 1
   * - Maximum: 43200 (30 days)
   */
  readonly accountLockoutDurationMinutes?: number | any;
}

/**
 * Guest usage properties.
 *
 * @remarks
 * Tracks guest user usage and billing for Azure AD B2B collaboration.
 */
export interface GuestUsageProps {
  /**
   * The tenant ID to track guest usage for.
   *
   * Constraints:
   * - Must be a valid GUID
   */
  readonly tenantId?: string;

  /**
   * The Azure subscription to link for billing.
   */
  readonly linkedSubscriptionId?: string;

  /**
   * The resource location.
   */
  readonly location?: string;

  /**
   * Additional guest usage tags.
   */
  readonly tags?: Record<string, string> | any;
}

/**
 * Properties for Microsoft.AzureActiveDirectory/b2cDirectories (L1 construct).
 *
 * @remarks
 * Microsoft.AzureActiveDirectory/b2cDirectories
 *
 * **ARM Resource Type**: `Microsoft.AzureActiveDirectory/b2cDirectories`
 * **API Version**: `2023-05-17-preview`
 *
 * Direct mapping to ARM resource properties with no defaults or transformations.
 *
 * Creates an Azure AD B2C directory for consumer identity management.
 */
export interface ArmB2CDirectoriesProps {
  /**
   * The name of the B2C directory resource.
   *
   * Constraints:
   * - Length: 1-260 characters
   * - Pattern: Can contain alphanumeric, underscore, hyphen, and period
   *
   * @example 'contoso-b2c-prod'
   */
  readonly name: string;

  /**
   * The resource location.
   *
   * @remarks
   * For B2C directories, this refers to the Azure resource location,
   * not the data residency location (which is set in properties.location).
   *
   * @example 'global', 'United States', 'Europe'
   */
  readonly location: string;

  /**
   * The SKU for the B2C directory.
   */
  readonly sku: B2CDirectorySku | any;

  /**
   * B2C directory properties.
   */
  readonly properties?: B2CDirectoryProps | any;

  /**
   * Resource tags.
   *
   * @remarks
   * Key-value pairs for organizing and categorizing resources.
   */
  readonly tags?: Record<string, string> | any;
}

/**
 * Properties for Microsoft.AzureActiveDirectory/ciamDirectories (L1 construct).
 *
 * @remarks
 * Microsoft.AzureActiveDirectory/ciamDirectories
 *
 * **ARM Resource Type**: `Microsoft.AzureActiveDirectory/ciamDirectories`
 * **API Version**: `2023-05-17-preview`
 *
 * Direct mapping to ARM resource properties with no defaults or transformations.
 *
 * Creates a Customer Identity and Access Management (CIAM) directory.
 */
export interface ArmCIAMDirectoriesProps {
  /**
   * The name of the CIAM directory resource.
   *
   * Constraints:
   * - Length: 1-260 characters
   * - Pattern: Can contain alphanumeric, underscore, hyphen, and period
   *
   * @example 'contoso-ciam-prod'
   */
  readonly name: string;

  /**
   * The resource location.
   *
   * @remarks
   * Must be a valid Azure region.
   *
   * @example 'eastus', 'westeurope'
   */
  readonly location: string;

  /**
   * The SKU for the CIAM directory.
   */
  readonly sku: CIAMDirectorySku | any;

  /**
   * CIAM directory properties.
   */
  readonly properties?: CIAMDirectoryProps | any;

  /**
   * Resource tags.
   */
  readonly tags?: Record<string, string> | any;
}

/**
 * Properties for Microsoft.AzureActiveDirectory/guestUsages (L1 construct).
 *
 * @remarks
 * Microsoft.AzureActiveDirectory/guestUsages
 *
 * **ARM Resource Type**: `Microsoft.AzureActiveDirectory/guestUsages`
 * **API Version**: `2021-04-01`
 *
 * Direct mapping to ARM resource properties with no defaults or transformations.
 *
 * Links an Azure subscription to track and bill for guest user usage.
 */
export interface ArmGuestUsagesProps {
  /**
   * The name of the guest usage resource.
   *
   * Constraints:
   * - Must be unique within the resource group
   *
   * @example 'guest-usage-tracking'
   */
  readonly name: string;

  /**
   * The resource location.
   *
   * @example 'global'
   */
  readonly location: string;

  /**
   * Guest usage properties.
   */
  readonly properties?: GuestUsageProps | any;

  /**
   * Resource tags.
   */
  readonly tags?: Record<string, string> | any;
}
