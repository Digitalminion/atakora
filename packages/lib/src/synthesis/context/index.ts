/**
 * Context management for context-aware ARM template synthesis.
 *
 * This module provides the SynthesisContext class, which enables resources
 * to generate correct ARM expressions based on their template assignments.
 *
 * @module synthesis/context
 */

export { SynthesisContext, ArmExpression } from './synthesis-context';
